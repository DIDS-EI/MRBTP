fallback
    condition IsIn(cutlets,microwave) & IsSwitchedOn(microwave)
    fallback
        sequence
            condition IsSelfTask(frozenset({'IsIn(cutlets,microwave)'}))
            action WalkToPutCutletsInMicrowave()
            action FinishTask()
        sequence
            condition IsLeftHandEmpty(agent-1) & IsOpen(microwave) & IsRightHandEmpty(agent-2) & IsSwitchedOff(microwave)
            action SelfAcceptTask(frozenset({'IsIn(cutlets,microwave)'}))
    sequence
        condition IsLeftHandEmpty(agent-1) & IsNear(agent-2,microwave) & IsOpen(microwave) & IsRightHolding(agent-2,cutlets) & IsSwitchedOff(microwave)
        action RightPutIn(agent-2,cutlets,microwave)
