fallback
    condition IsIn(milk,box)
    fallback
        condition IsOpen(box) & IsRightHandEmpty(agent-1)
        fallback
            sequence
                condition IsSelfTask(frozenset({'IsOpen(box)'}))
                action WalkToOpenBox()
                action FinishTask()
            sequence
                condition IsClose(box) & IsLeftHandEmpty(agent-0) & IsRightHandEmpty(agent-1)
                action SelfAcceptTask(frozenset({'IsOpen(box)'}))
