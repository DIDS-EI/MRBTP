fallback
    condition IsOpen(stove)
    fallback
        sequence
            condition IsSelfTask(frozenset({'IsOpen(stove)'}))
            action WalkToOpenStove()
            action FinishTask()
        sequence
            condition IsClose(stove) & IsLeftHandEmpty(agent-0)
            action SelfAcceptTask(frozenset({'IsOpen(stove)'}))
