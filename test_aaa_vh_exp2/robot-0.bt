fallback
    condition IsOn(chicken,kitchentable)
    fallback
        sequence
            condition IsSelfTask(frozenset({'IsOn(chicken,kitchentable)'}))
            action WalkToPutCakeTable()
            action FinishTask()
        sequence
            condition IsRightHandEmpty(agent-0)
            action SelfAcceptTask(frozenset({'IsOn(chicken,kitchentable)'}))
    sequence
        condition IsNear(agent-0,kitchentable) & IsRightHolding(agent-0,chicken)
        action RightPut(agent-0,chicken,kitchentable)
