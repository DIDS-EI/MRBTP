fallback
    condition 1 & 2
    fallback
        sequence
            condition IsSelfTask(1) id=0 act=CMP_A1_D0
            action CMP_A1_D0()
            action FinishTask()
        sequence
            fallback
                condition 2
                sequence
                    condition 10
                    action A2_D0_Sub3 pre: (10) add: (2)  del: ()
            action SelfAcceptTask(1) id=0 act=CMP_A1_D0
    sequence
        condition 1 & 10
        action A2_D0_Sub3 pre: (10) add: (2)  del: ()
    fallback
        sequence
            condition IsSelfTask(1) id=1 act=CMP_A1_D0
            action CMP_A1_D0()
            action FinishTask()
        sequence
            action SelfAcceptTask(1) id=1 act=CMP_A1_D0
