fallback
    condition NumCondition(0) & NumCondition(1) & NumCondition(3) & NumCondition(4) & NumCondition(9)
    sequence
        condition NumCondition(0) & NumCondition(1) & NumCondition(4)
        action NumAction(A1(0))
    sequence
        condition NumCondition(4) & NumCondition(7)
        action NumAction(A1(0))
